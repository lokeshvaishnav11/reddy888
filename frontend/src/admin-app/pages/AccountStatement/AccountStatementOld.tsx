import React from 'react'
import AccountStatementAdmin from './AccountStatementAdmin'

const AccountStatementOld = () => {
  // Reuse the existing AccountStatementAdmin component to show the same table/layout
  return <AccountStatementAdmin />
}

export default AccountStatementOld
